<h2> Which film do you want to add?</h2>
<h4> You're adding to <%= @list.name %> list</h4>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.association :movie, as: :radio_buttons %>
  <%= f.input :comment %>
  <%= f.submit %>
<% end %>
